[package]
name = "slate-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"
glob = "0.3"
grep-regex = "0.1"
grep-searcher = "0.1"
ignore = "0.4"
crossbeam-channel = "0.5"
tracing = "0.1"
